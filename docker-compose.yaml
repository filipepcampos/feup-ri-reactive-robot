version: '2'

services:
  ros-it:
    build: .
    container_name: ros-it
    environment:
      - "DISPLAY=${DISPLAY}"
      - "QT_X11_NO_MITSHM=1" #fix some QT bugs
    volumes: 
      #share your x11 socket and permissions to the container
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ./ws/:/ws # mount the current directory to the container workspace
    stdin_open: true 
    tty: true
